name: 'Action - RottenVille Bot Development'
description: 'Actions for PULL Requests'
author: 'Kyonax'

inputs:
  GITHUB_TOKEN:
    description: 'GitHub token'
    required: true
  TENOR_TOKEN:
    description: 'Tenor API Token'
    required: true

runs:
  using: 'node12'
  main: 'dist/index.js'
